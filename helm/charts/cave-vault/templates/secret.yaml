apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cave.fullname" . }}
  labels:
    {{- include "cave.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Placeholder — populate via External Secrets Operator or sealed-secrets in production.
  placeholder: "change-me"
