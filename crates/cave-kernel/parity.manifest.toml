# parity.manifest.toml — cave-kernel
# First-party primitives crate. No external upstream — these are the
# shared infrastructure types (event bus, scheduler hooks, supervisor
# trees) consumed by every cave-* module.
#
# Stubbed 2026-05-02.

[upstream]
org     = "cave-runtime"
repo    = "cave-runtime"
version = "v0.1.0"

[module]
name        = "cave-kernel"
description = "Shared kernel primitives — first-party infrastructure"
source_root = "src"
first_party = true

[parity]
infra_only = true
ratio       = 0.0        # honest: 0.0 means upstream parity NOT yet measured
last_audit  = "2026-05-12"

