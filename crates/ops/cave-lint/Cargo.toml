[package]
name = "cave-lint"
description = "Config & image linting — compatible with Hadolint + Checkov + Pluto + kubent"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
axum = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
proptest = "1"
