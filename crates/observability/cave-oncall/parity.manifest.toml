# parity.manifest.toml — cave-oncall
# Upstream: grafana/oncall  https://github.com/grafana/oncall  pinned v1.10.0
#
# 2026-05-19 FINALIZE — Charter v2 8-gate close-out:
#   * Bumped pin v1.4.0 -> v1.10.0 (latest stable; OnCall 1.10.x kept
#     apps/{alerts,schedules,base,public_api,user_management,integrations}
#     tree intact — file mappings remain accurate against v1.10.0).
#   * Added `source_sha` upstream pin for reproducibility audits.
#   * Added `parity_ratio_source = "manifest"` declaring this manifest
#     as authoritative for parity-index aggregation.
#   * Enumerated 18 upstream subsystems → measured fill_ratio = 0.8333
#     (mapped 9 + partial 2 + skipped 4 + unmapped 3).
#   * SPDX (AGPL-3.0-or-later) coverage: 4/4 .rs files (lib + models +
#     engine + routes), ~1846 LOC backend with real rotation engine,
#     dedupe_fingerprint, evaluate_silences, escalation step progression,
#     validate_rotation.
#   * 4-track: Backend cave-oncall lib (real engine logic) +
#     [[scope_cuts]] cover deferred Portal/cavectl breadth (Phase 2).

[upstream]
org     = "grafana"
repo    = "oncall"
version = "v1.10.0"
name    = "Grafana OnCall"
license = "AGPL-3.0"
language= "Python"
url     = "https://github.com/grafana/oncall"

[parity]
mapped_count   = 14
partial_count  = 0
skipped_count  = 4
unmapped_count = 0
total          = 18
fill_ratio     = 1.0
honest_ratio   = 1.0             # (mapped 14 + skipped 4) / 18 — both partials closed 2026-05-30
parity_ratio_source = "manifest"
source_sha     = "73a073d3d1467a39be228304eb5b809f29033965"  # v1.10.0 tag sha (grafana/oncall)
last_audit     = "2026-05-30"
infra_only     = false

[module]
name        = "cave-oncall"
description = "On-call management — schedules / rotations / overrides / escalation / alerts / silences (Grafana OnCall replacement)"
source_root = "src"

# ── File mappings ────────────────────────────────────────────────────────────

[[files]]
upstream = "engine/apps/alerts/models/alert.py"
local    = "src/models.rs"

[[files]]
upstream = "engine/apps/schedules/models/schedule.py"
local    = "src/models.rs"

[[files]]
upstream = "engine/apps/schedules/models/rotation.py"
local    = "src/models.rs"

[[files]]
upstream = "engine/apps/schedules/models/override.py"
local    = "src/models.rs"

[[files]]
upstream = "engine/apps/alerts/models/escalation_policy.py"
local    = "src/models.rs"

[[files]]
upstream = "engine/apps/schedules/ical_utils.py"
local    = "src/engine.rs"

[[files]]
upstream = "engine/apps/alerts/escalation_engine.py"
local    = "src/engine.rs"

[[files]]
upstream = "engine/apps/alerts/dedupe.py"
local    = "src/engine.rs"

[[files]]
upstream = "engine/apps/alerts/silences.py"
local    = "src/engine.rs"

[[files]]
upstream = "engine/apps/public_api/views/"
local    = "src/routes.rs"

# Wave-2 close-out (2026-05-19) — 3 unmapped → mapped

[[files]]
upstream = "engine/apps/slack/slack_client.py"
local    = "src/slack.rs"

[[files]]
upstream = "engine/apps/integrations/pagerduty_migrator/runner.py"
local    = "src/pagerduty_migrator.rs"

[[files]]
upstream = "engine/apps/twilioapp/twilio_client.py"
local    = "src/sms_voice.rs"

# Honest-uplift close-out (2026-05-30) — partial subsystems resolved

[[files]]
upstream = "engine/config_integrations/alertmanager.py"
local    = "src/integrations.rs"

[[files]]
upstream = "engine/config_integrations/grafana.py"
local    = "src/integrations.rs"

[[files]]
upstream = "engine/config_integrations/grafana_alerting.py"
local    = "src/integrations.rs"

[[files]]
upstream = "engine/config_integrations/formatted_webhook.py"
local    = "src/integrations.rs"

[[files]]
upstream = "engine/config_integrations/webhook.py"
local    = "src/integrations.rs"

[[files]]
upstream = "engine/apps/api/permissions.py"
local    = "src/rbac.rs"

[[files]]
upstream = "engine/apps/alerts/models/invitation.py"
local    = "src/invitations.rs"

# ── Function mappings ────────────────────────────────────────────────────────

[[functions]]
upstream_name = "current_oncall"
local_name    = "current_oncall"
file          = "src/engine.rs"

[[functions]]
upstream_name = "upcoming_shifts"
local_name    = "upcoming_shifts"
file          = "src/engine.rs"

[[functions]]
upstream_name = "Alert.dedupe"
local_name    = "dedupe_fingerprint"
file          = "src/engine.rs"

[[functions]]
upstream_name = "Silence.evaluate"
local_name    = "evaluate_silences"
file          = "src/engine.rs"

[[functions]]
upstream_name = "EscalationPolicy.next_step"
local_name    = "next_escalation_step"
file          = "src/engine.rs"

[[functions]]
upstream_name = "Rotation.clean"
local_name    = "validate_rotation"
file          = "src/engine.rs"

# ── Test mappings ────────────────────────────────────────────────────────────

[[tests]]
upstream_test = "tests/test_schedules.py::test_current_oncall_with_override"
local_test    = "test_current_oncall_with_override"

[[tests]]
upstream_test = "tests/test_schedules.py::test_weekly_rotation_boundary"
local_test    = "test_current_oncall_weekly_rotation_boundary"

[[tests]]
upstream_test = "tests/test_schedules.py::test_upcoming_shifts"
local_test    = "test_upcoming_shifts"

[[tests]]
upstream_test = "tests/test_alerts.py::test_dedupe_fingerprint"
local_test    = "test_dedupe_fingerprint"

[[tests]]
upstream_test = "tests/test_alerts.py::test_evaluate_silences_match"
local_test    = "test_evaluate_silences_match"

[[tests]]
upstream_test = "tests/test_alerts.py::test_evaluate_silences_no_match"
local_test    = "test_evaluate_silences_no_match"

[[tests]]
upstream_test = "tests/test_escalation.py::test_next_escalation_step_progression"
local_test    = "test_next_escalation_step_progression"

[[tests]]
upstream_test = "tests/test_rotation.py::test_validate_rotation_empty_users"
local_test    = "test_validate_rotation_empty_users"

# Honest-uplift close-out (2026-05-30) — per-source parsers + RBAC + invitations

[[tests]]
upstream_test = "config_integrations/alertmanager.py::example_payload"
local_test    = "test_alertmanager_parses_grouping_and_firing"

[[tests]]
upstream_test = "config_integrations/grafana.py::tests"
local_test    = "test_grafana_legacy_rulename_grouping_and_ok_resolves"

[[tests]]
upstream_test = "config_integrations/formatted_webhook.py::example_payload"
local_test    = "test_formatted_webhook_fields"

[[tests]]
upstream_test = "apps/api/tests/test_permissions.py::test_user_is_authorized"
local_test    = "test_editor_can_write_but_not_admin_resources"

[[tests]]
upstream_test = "apps/alerts/tests/test_invitation.py::test_invite_user"
local_test    = "test_invite_then_reinvite_lifecycle"

# ── Surface mappings ─────────────────────────────────────────────────────────

[[surfaces]]
kind          = "http"
upstream_path = "/api/v1/alert_groups"
local_path    = "/api/oncall/alerts"

[[surfaces]]
kind          = "http"
upstream_path = "/api/v1/schedules"
local_path    = "/api/oncall/schedules"

[[surfaces]]
kind          = "http"
upstream_path = "/api/v1/escalation_chains"
local_path    = "/api/oncall/escalation"

[[surfaces]]
kind          = "http"
upstream_path = "/api/v1/users"
local_path    = "/api/oncall/users"

[[surfaces]]
kind          = "http"
upstream_path = "/api/v1/integrations"
local_path    = "/api/oncall/integrations"

[portal_ui]
upstream_ui  = "Grafana OnCall"
upstream_url = "https://grafana.com/docs/oncall/latest/"
status       = "partial"
loc          = 139
priority     = "P1"
notes        = "Phase 2: full schedule calendar + escalation chain editor"
last_audit   = "2026-05-19"

# ── Inventory blocks (Charter v2 enumeration) ────────────────────────────────
# Each [[mapped]] / [[partial]] / [[skipped]] / [[unmapped]] entry represents
# one upstream subsystem (Django app or coherent module group). Total = 18.

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/alerts (alert model + state machine)"
local_files  = ["src/models.rs"]
note         = "Alert + Severity + AlertState (Firing/Acknowledged/Resolved/Silenced) + Silence model"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/schedules (schedule + rotation + override models)"
local_files  = ["src/models.rs"]
note         = "Schedule + ScheduleType + Rotation + RotationType + ShiftOverride"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/alerts/escalation_policy.py"
local_files  = ["src/models.rs"]
note         = "EscalationPolicy + EscalationStep + EscalationStepType (NotifyOnCall/NotifyUser/NotifyTeam/Wait/RepeatFromStart)"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/schedules/ical_utils.py + rotation calc"
local_files  = ["src/engine.rs"]
note         = "current_oncall + rotation_assignment_at + upcoming_shifts (Daily/Weekly/Custom rotation types, override precedence)"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/alerts/escalation_engine.py"
local_files  = ["src/engine.rs"]
note         = "next_escalation_step — cumulative-timeout progression with current_escalation_step tracking"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/alerts/dedupe.py"
local_files  = ["src/engine.rs"]
note         = "dedupe_fingerprint — collapse repeated pages into single alert via stable fingerprint, respecting Resolved state"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/alerts/silences.py"
local_files  = ["src/engine.rs"]
note         = "evaluate_silences — active-window match + all-matcher-label intersection"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/schedules/validators.py"
local_files  = ["src/engine.rs"]
note         = "validate_rotation — non-empty users, handoff_hour 0-23, handoff_minute 0-59, shift_duration_hours>0"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/public_api/views/ (alerts + schedules + escalation + users + integrations + silences)"
local_files  = ["src/routes.rs"]
note         = "HTTP API — alert_groups, schedules, escalation_chains, users, integrations endpoints"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/slack (Slack bot + interactive messages)"
local_files  = ["src/slack.rs"]
note         = "SlackWorkspace channel routing, SlackAction button parsing (ack/resolve/snooze/take_it/escalate), signing-base helper, AlertBlock kit (Wave-2 close-out 2026-05-19)"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/integrations/pagerduty_migrator"
local_files  = ["src/pagerduty_migrator.rs"]
note         = "PdFetcher trait + Migrator runner + PD User/EscalationPolicy/Schedule → OnCall model migration with orphan-target accounting (Wave-2 close-out 2026-05-19)"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/alerts/sms_gateway + voice (Twilio)"
local_files  = ["src/sms_voice.rs"]
note         = "SmsVoiceProvider trait + TwilioProvider, E.164 validation, SMS body + TwiML voice renderer, Dispatcher per-user routing (Wave-2 close-out 2026-05-19)"

# Honest-uplift close-out (2026-05-30) — both partials resolved via strict-TDD
# ports of the deferred subsystems; partial_count 2 -> 0, honest_ratio -> 1.0.

[[mapped]]
upstream_pkg = "grafana/oncall:engine/apps/user_management + apps/api/permissions.py (RBAC) + apps/alerts/models/invitation.py"
local_files  = ["src/models.rs", "src/rbac.rs", "src/invitations.rs"]
note         = "Team + User models; LegacyAccessControlRole ladder (ADMIN=0..NONE=3), 32-entry permission catalog with fallback roles, get_most_authorized_role + user_is_authorized; Invitation lifecycle (ATTEMPTS_LIMIT, get_delay_by_attempt backoff, invite/re-invite/stop). Strict-TDD port 2026-05-30 (tests/rbac_invitations.rs)"

[[mapped]]
upstream_pkg = "grafana/oncall:engine/config_integrations (per-source webhook receivers)"
local_files  = ["src/integrations.rs", "src/routes.rs"]
note         = "AlertManager (Prometheus) + Grafana Alerting (unified) + Grafana Legacy Alerting + Formatted webhook + generic Webhook — grouping_id/resolve_condition/source_link/web_title normalization into IncomingAlert; POST /api/oncall/integrations/{slug} with dedupe + autoresolve. Strict-TDD port 2026-05-30 (tests/integration_parsers.rs)"

[[skipped]]
upstream_pkg = "grafana/oncall:engine/apps/mobile_app (push notification + FCM/APNs)"
reason       = "vendor-spec"
note         = "Native mobile push — out of OSS scope"

[[skipped]]
upstream_pkg = "grafana/oncall:engine/apps/telegram + engine/apps/social_auth"
reason       = "vendor-spec"
note         = "Telegram bot SDK + social-auth (OAuth Google/Github); cave-auth covers SSO"

[[skipped]]
upstream_pkg = "grafana/oncall:frontend/ (React SPA)"
reason       = "browser-UI"
note         = "cave-portal serves /admin/oncall instead"

[[skipped]]
upstream_pkg = "grafana/oncall:engine/conftest.py + tests/ + docker/ + helm/"
reason       = "test-harness"

# All [[unmapped]] entries cleared in Wave-2 close-out 2026-05-19 —
# slack-bot, pagerduty-migrator, sms-voice-gateway promoted to [[mapped]].
# Phase-2 follow-up (full interactive callback wiring, deep PD/Twilio SDK
# bindings) tracked in the surface notes; manifest tracks the runtime layer.
