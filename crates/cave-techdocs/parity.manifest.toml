# parity.manifest.toml — cave-techdocs
# Upstream tracked via parent crate cave-portal (backstage/backstage).
# Backstage TechDocs is a plugin shipped in the same monorepo.
#
# Stubbed 2026-05-02. Bump dispatch happens against cave-portal.

[upstream]
org     = "backstage"
repo    = "backstage"
version = "v1.50.4"
source_sha = "v1.50.4"  # G1: tag-based pin (deep-port may override)
name    = "Backstage"
license = "Apache-2.0"
language= "TypeScript"
url     = "https://github.com/backstage/backstage"
[module]
name        = "cave-techdocs"
description = "TechDocs (Backstage docs-as-code) — sub-crate of cave-portal"
source_root = "src"
parent_module = "cave-portal"

[parity]
infra_only = true
ratio       = 0.0        # honest: 0.0 means upstream parity NOT yet measured
last_audit  = "2026-05-12"

