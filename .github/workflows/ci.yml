name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  check:
    name: Check & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2

      - name: Format check
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --all-targets --all-features

      - name: Test
        run: cargo test --all

      - name: Build release
        run: cargo build --release --bin cave-runtime

  upstream-check:
    name: Upstream Tracker
    runs-on: ubuntu-latest
    # Run weekly on Monday at 06:00 UTC
    # Also triggered manually
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4
      - name: Check upstream releases
        run: |
          echo "TODO: Run cave-upstream binary to check all 26 tracked projects"
          echo "TODO: AI triage via Claude API"
          echo "TODO: Create GitHub issues for ADOPT items"
