# Postgres-parity OLTP
groups:
  - name: cave-pg-slo
    interval: 30s
    rules:
      - alert: PgSloBurnRateFast
        expr: |
          (
            sum(rate(pg_total{job="cave-pg",code=~"5.."}[1h]))
            /
            sum(rate(pg_total{job="cave-pg"}[1h]))
          ) > (14.4 * 0.001)
        for: 2m
        labels:
          severity: critical
          tenant_scoped: "true"
        annotations:
          summary: "cave-pg fast burn-rate (1h)"
          description: "5xx fraction is {{ $value | humanizePercentage }} — burning the 99.9% budget 14.4× faster than allowed."
          runbook_url: "https://docs.cave.dev/runbooks/cave-pg/slo-burn-fast.md"
      - alert: PgSloBurnRateSlow
        expr: |
          (
            sum(rate(pg_total{job="cave-pg",code=~"5.."}[6h]))
            /
            sum(rate(pg_total{job="cave-pg"}[6h]))
          ) > (6 * 0.001)
        for: 15m
        labels:
          severity: warning
          tenant_scoped: "true"
        annotations:
          summary: "cave-pg slow burn-rate (6h)"
          description: "Sustained 5xx fraction over 6h."
          runbook_url: "https://docs.cave.dev/runbooks/cave-pg/slo-burn-slow.md"
      - alert: PgErrorBudgetLow
        expr: |
          (
            1 -
            (sum(rate(pg_total{job="cave-pg",code=~"5.."}[7d]))
              / sum(rate(pg_total{job="cave-pg"}[7d])))
          ) < 0.999
        for: 30m
        labels:
          severity: warning
        annotations:
          summary: "cave-pg 7-day availability < 99.9%"
          description: "Trailing 7d availability = {{ $value | humanizePercentage }}."
          runbook_url: "https://docs.cave.dev/runbooks/cave-pg/error-budget.md"
      - alert: PgLatencyP99High
        expr: |
          histogram_quantile(
            0.99,
            sum(rate(pg_duration_seconds_bucket{job="cave-pg"}[5m])) by (le)
          ) > 1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "cave-pg p99 latency > 1s"
          description: "p99 = {{ $value }}s."
          runbook_url: "https://docs.cave.dev/runbooks/cave-pg/latency.md"
      - alert: PgSaturationHigh
        expr: 'sum(pg_inflight_requests{job="cave-pg"}) > 400'
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "cave-pg inflight requests > 400"
          description: "Concurrent requests = {{ $value }}."
          runbook_url: "https://docs.cave.dev/runbooks/cave-pg/saturation.md"
      - alert: PgMemoryPressure
        expr: 'process_resident_memory_bytes{job="cave-pg"} > 4 * 1024 * 1024 * 1024'
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "cave-pg RSS > 4 GiB"
          runbook_url: "https://docs.cave.dev/runbooks/cave-pg/memory.md"
      - alert: PgCardinalityExplosion
        expr: 'count(count by (tenant_id) (pg_total{job="cave-pg"})) > 5000'
        for: 30m
        labels:
          severity: warning
        annotations:
          summary: "cave-pg tenant cardinality > 5000"
          description: "Count = {{ $value }}."
          runbook_url: "https://docs.cave.dev/runbooks/cave-pg/cardinality.md"
      - alert: PgHealthProbeFailing
        expr: 'min(up{job="cave-pg"}) < 1'
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "cave-pg health probe failing"
          description: "At least one instance reports up == 0."
          runbook_url: "https://docs.cave.dev/runbooks/cave-pg/health.md"
