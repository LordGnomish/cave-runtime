# Sweep-002 merge plan + Sweep-003 candidate matrix

**Author:** Plan agent (2026-04-23 session)
**Status:** Approved for implementation
**Paralel çalışan:** cave-apiserver tests, cave-kubelet tests, Qwen3 prompt whip
**Owner branch:** `merge/sweep-002-integration` (to be created)

## 1. Current state

- **Sweep-001 ✅ merged** (WAL + Labels, 2026-04-21)
- **Sweep-002 ⏸ pending merge** (refactor/continuous-synergy, 36 commits ahead)
  - 5 primitives extracted: `cave-kernel::{raft, event, reconcile, netns}` + `cave-crypto::spiffe`
  - 122 new tests, +4777 LOC, ~85% coverage
- **cave-kernel bugün:** 13 modules (audit/authn/circuit/event/http/lease/mvcc/netns/notification/parity/raft/reconcile/store/watch)

## 2. Merge conflict analysis

| Area | Risk | Strategy |
|------|------|----------|
| cave-kernel new modules | SAFE | clean merge (main hasn't touched cave-kernel) |
| cave-apiserver::authn re-export | LOW | 2-way combine |
| **cave-local-llm (Qwen3 multi-repo queue)** | **HIGH** | Accept main (`--theirs crates/cave-local-llm/`) — Qwen3 is fresher |
| cave-crypto (new crate) | SAFE | new Cargo.toml, no conflict |

## 3. Merge execution sequence

```bash
# Integration branch
git checkout -b merge/sweep-002-integration main

# Rebase sweep-002 on latest main (merge base: 11c62433)
git rebase --onto main 11c62433d717bd6e6994029a208106f6fcd4ff90 \
  refactor/continuous-synergy

# Expected conflicts in cave-local-llm — accept main
git checkout --theirs crates/cave-local-llm/

# Gate: all cave-kernel + cave-crypto tests must pass
cargo test --lib cave_kernel --release
cargo test --lib cave_crypto --release
cargo check -p cave-apiserver -p cave-ha -p cave-etcd

# Fast-forward to main only after all green
```

**No `git push --force`. No `git reset --hard`.**

## 4. Multi-tenant compliance gap (ADR-MULTI-TENANT-001)

| Primitive | tenant_id today | Fix required |
|-----------|-----------------|--------------|
| raft state machine | generic | ✅ safe (caller carries tenant) |
| EventBus<E> | generic E | ✅ safe + doc note |
| Controller<T> reconcile | generic T | ⚠ add `enqueue_with_tenant(key, tenant_id, item)` builder |
| SPIFFE SVID | embedded in trust domain path | ✅ already compliant |
| netns::EbpfHook | missing | ⚠ add `tenant_id: String` field |
| netns::CgroupV2Handle | missing | ⚠ add `tenant_id: String` field |

**Non-breaking additive updates.** Follow-up commit after sweep-002 merge.

## 5. Sweep-003 candidate matrix

| # | Candidate | Duplicate sites | Risk | Effort | Priority | Action |
|---|-----------|-----------------|------|--------|----------|--------|
| S3-01 | **RateLimiter (token-bucket)** | cave-mesh, cave-llm-gateway, cave-gateway | LOW | MEDIUM | **P2 — next sweep** | Extract to `cave-kernel::rate_limit::RateLimiter<(TenantId, K)>` |
| S3-02 | TLS terminator hybrid PQC | cave-gateway, cave-apiserver, cave-kubelet, cave-mesh | HIGH | HIGH | P1 but BLOCKED | Audit-only pre-OSS; defer to sweep-004 |
| S3-03 | Cache + TTL | cave-cache, cave-llm-gateway, cave-dns | LOW | MEDIUM | P4 | Post-OSS |
| S3-07 | Leader election | cave-ha, cave-apiserver (etcd-based), cave-kamaji | HIGH | HIGH | P1 BLOCKED | cave-etcd TDD active; defer |
| S3-04/05/06/08/09/10 | Misc | various | LOW | LOW | Skip | No meaningful synergy |

## 6. 28-day pre-OSS roadmap

| Day | Track | Task |
|-----|-------|------|
| 4/23–4/25 | synergy | sweep-002 merge to main (with conflict resolution) |
| 4/26–4/28 | security | TLS PQC audit + ADR spec (informational) |
| 4/29–5/03 | synergy | sweep-003 RateLimiter TDD |
| 5/04–5/07 | hardening | multi-tenant fields (netns, reconcile) + tests |
| 5/08–5/12 | integration | tests + docs + sweep-003 migration |
| 5/13–5/15 | synergy | sweep-003 merge |
| 5/16–5/21 | pre-OSS gate | tenant-enforcement audit + portal integration |

## 7. Critical files

### Merge-002
- `crates/cave-kernel/src/lib.rs` — confirm 13 modules
- `crates/cave-crypto/src/lib.rs` — new crate
- `.claude/worktrees/gallant-raman-74584e/docs/synergy/sweep-002-progress.md` — source-of-truth batches

### Sweep-003 (RateLimiter)
- `crates/cave-mesh/src/rate_limit.rs` — target for migration
- `crates/cave-llm-gateway/src/rate_limit.rs` — target for migration
- `crates/cave-gateway/src/plugins/rate_limiting.rs` — Kong-sliding-window; coexist, don't merge

### Multi-tenant fields
- `crates/cave-kernel/src/netns.rs` — add `tenant_id` to EbpfHook, CgroupV2Handle
- `crates/cave-kernel/src/reconcile.rs` — add `enqueue_with_tenant` builder

## 8. Immediate next step

**Create branch `merge/sweep-002-integration`, run rebase sequence in §3, gate on tests in §3.** Implementation task dispatched alongside this doc commit.
