# OSS Hijyen Report — 2026-05-02

Generated by `scripts/oss-hijyen-prep.sh` (dry-run; no files modified).
See ADR-148 for the launch strategy this report feeds.


## 1. Secret scanners

⚠️ `trufflehog` not installed — skipping. Install with `brew install trufflehog` before Faz 1 sprint.

⚠️ `gitleaks` not installed — skipping. Install with `brew install gitleaks` before Faz 1 sprint.

## 2. .DS_Store files

Found **1** `.DS_Store` files.

Sample (first 10):
```
./.claude/.DS_Store
```

**Faz 1 fix:** `find . -name '.DS_Store' -delete`

## 3. Build artefact directories (should be gitignore'd)

✓ No build artefacts tracked in git.

## 4. Path leakage — `/Users/<user>/`

Files containing `/Users/<user>`: **10**

Top 30 by file (path → line count):
```
queue/runtime-tasks.yaml:129
deploy/launchd/com.caveruntime.local-llm-daemon.plist:7
scripts/oss-hijyen-prep.sh:6
docs/oss-prep/hijyen-report-2026-05-02.md:2
docs/adr/ADR-148_OSS_Launch_History_Strategy.md:2
docs/parity/wave3-fill.py:1
docs/parity/wave3-calc.py:1
docs/parity/wave3-branch-sweep.sh:1
docs/parity/cave-net-2026-04-29-merge-halt-note.md:1
docs/local-llm/README.md:1
```

**Faz 1 fix:** sed-replace with `$HOME` or `~/`. Audit each hit — some are example paths in docs which is fine; others are baked-in plist / launchctl paths that need env-var indirection.

## 5. Email leakage

Files containing `btartan@gmail.com`: **2**

All hits:
```
scripts/oss-hijyen-prep.sh:3
docs/oss-prep/hijyen-report-2026-05-02.md:1
docs/adr/ADR-148_OSS_Launch_History_Strategy.md:1
```

Other emails in working tree (sample, top 30 distinct):
```
a@a.com
a@x.com
admin@cave.local
admin@caveplatform.dev
admin@platform.cave
admin@tenant1.cave
alice@acme.com
alice@mycompany.com
bob@techcorp.com
btartan@gmail.com
contributors@caveplatform.dev
debian-glibc@lists.debian.org
dev@cave.local
hello@ex.com
johanna@acme.com
john@acme.com
password@billing-db-portability-test.postgres.database.azure.com
platform-security@caveplatform.dev
pwd@postgres.db
pwd@postgres.default
reflex-engine@cave.dev
s3cr3t@db.example.com
shopper@db.local
ssh-ed25519-cert-v01@openssh.com
test@email.com
test@test.com
timo.teras@iki.fi
u@t.com
user3@other.org
user@test.com
```

**Faz 1 fix:** decide per-file: replace personal with `noreply@cave-runtime.org` placeholder, or mark intentional (third-party in docs).

## 6. Commit history Co-Authored-By trailers (informational only)

Orphan-launch (ADR-148) discards commit history; this section is for **CREDITS.md** input, not for scrubbing.

Top Co-Authored-By trailers (count → trailer):
```
 295 Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

## 7. Compliance / OSS launch required files

- ✓ `LICENSE` exists
- ✗ `NOTICE` **missing**
- ✓ `README.md` exists
- ✗ `CREDITS.md` **missing**
- ✓ `CONTRIBUTING.md` exists
- ✗ `CODE_OF_CONDUCT.md` **missing**
- ✓ `SECURITY.md` exists
- ✗ `docs/HISTORY.md` **missing**

**Faz 1 fix:** create the missing files before launch. ADR-148 verification gate #4 requires all of them.

## 8. Cargo.toml license / repository fields

- crates without explicit `license =` line (may inherit from workspace): 6
- crates with `license.workspace = true`: 97
- crates without `repository` reference: 10

**Faz 1 fix:** confirm root `Cargo.toml` has `[workspace.package] license = "Apache-2.0" repository = "…"`; nudge stragglers to inherit.

## Summary

**Total leak-candidate count:** 17

Breakdown:
- .DS_Store files: 1
- Build artefacts tracked: 0
- Files with `/Users/<user>`: 10
- Files with `btartan@gmail.com`: 2
- Compliance files missing: 4

Per ADR-148, none of these block the orphan-launch strategy on the *history* side, but **all of them must be fixed in the working tree** during the Faz 1 hygiene sprint (2026-05-14 → 2026-05-19) before the launch commit is taken.
